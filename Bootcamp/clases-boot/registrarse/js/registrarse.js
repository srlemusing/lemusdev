
//Damos inicio a nuestra interaccion con JavaScript.

    var capturar = function()  //creamos la variable capturar anexo a la declararla como funcion.
    { 
        var nom = document.getElementById("minombre").value //creamos la variable nom para relacionar el id minombre para guardar el nombrel del usuario
        console.log(nom)
    }
